---
title: 'Widgets overview'
description: 'Conceptual overview — not an endpoint, must be skipped by the generator.'
---

Widgets are the core object of the Fixture API. This page has no `api:` frontmatter,
so it should never produce an operation in the spec.
